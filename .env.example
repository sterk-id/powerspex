# Local development defaults to SQLite when DATABASE_URI is left empty.
# Production must use a PostgreSQL connection string.
DATABASE_URI=

# Generate a unique value of at least 32 characters; never commit the real secret.
PAYLOAD_SECRET=replace-with-a-long-random-secret

# Public canonical origin, without a trailing slash.
NEXT_PUBLIC_SERVER_URL=http://localhost:3000
